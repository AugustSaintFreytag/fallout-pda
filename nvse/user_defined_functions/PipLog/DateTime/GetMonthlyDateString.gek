begin function { short iDay }

	; Returns a monthly-recurring date pattern in *-*-DD format.
	; Matches the given day number regardless of year or month.
	; Caller is responsible for Sv_Destruct on the returned string.

	string_var sDay = $iDay

	if Sv_Length sDay < 2
		let sDay := Sv_Construct "0%z" sDay
	endif

	string_var sResult = Sv_Construct "*-*-%z" sDay
	SetFunctionValue sResult

	Sv_Destruct sDay

end
